#include "main.h"
#include <stdio.h>

/**
 * set_string - sets value of a pointer to a char
 *
 * Return: 0 (success)
 */
void set_string(char **s, char *to)
{
	*ptr = &value;
}
int main(void)
{
	char character = 'T';
	char *charPointer = NULL;

	printf("before: charPointer = %p\n", (void *)charPointer);

	set_char(&charPointer, character);

	printf("After: charPointer = %p, *charPointer = %c\n",(charPointer, *charPointer);

			return (0);
			}
			
